export interface NameInfo {
  first_name: string;
  last_name: string;
}

export interface ContactInfo {
  phone_number: string;
  country_code: string;
  email: string;
}

export interface Gendered {
  gender: string;
}
